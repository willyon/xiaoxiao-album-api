# 审美模型会话记录（精简版）

当前方案：仅依赖 SigLIP 图像编码器 + 轻量回归头（ONNX）输出 0–100 审美分，不再依赖 NIMA。

## 已完成工作

- 建立 SigLIP embedding 抽取与缓存流程。
- 训练 MLP 回归头、导出 `siglip_aesthetic_head.onnx`，并生成 `training_report.json` 与 `predictions_vs_scores.csv`。

## 关键命令示例

详见 `data/aesthetic_head_musiq/README.md` 与训练脚本 `scripts/train_aesthetic_head.py`。

## 后续计划

- 继续迭代回归头结构与超参，提升相关性指标。
- 集成 SigLIP + 回归头到上线推理链路（已完成）。

## 备注

- 目前不再加载 NIMA；如需回溯历史比较，可参考 `data/aesthetic_head_musiq/predictions_vs_scores.csv`。
