const CLEANUP_TYPES = {
  DUPLICATE: "duplicate",
  SIMILAR: "similar",
  BLURRY: "blurry",
};

const CLEANUP_TYPE_LIST = Object.values(CLEANUP_TYPES);

module.exports = {
  CLEANUP_TYPES,
  CLEANUP_TYPE_LIST,
};
